{% extends "BZUserBundle::layout.html.twig"%}

{% block content_body %}
<form  class="ui-draggable" style="position: relative; left: 20%; margin-top: 2%;" action="{{ path('bz_user_update')}}" method="post" {{ form_enctype(edit_form) }} >
<div class="col-lg-6">
    <div class="panel panel-warning">
        <div class="panel-heading">
            MISE A JOUR DU COMPTE
        </div>
        <div class="panel-body">
          
          {{ form_widget(edit_form) }}
        </div>
        <div class="panel-footer">
            <a type="button" class="btn btn-sm btn-default " href="{{ path('pub_acceuil')}}" id="retour" ><i class="icon-arrow-left"> </i> Retour</a>
                  <button type="submit" class="btn btn-sm  btn-warning" >Mettre à jour</button>
                  <span class='tachat_img hide'><img src="{{ asset('/images/712.GIF') }}" alt="Second slide" class='img-circle' style="height: 20px;width: 20px;"> Enregistrement ...</span>
        </div>
    </div>
</div>
{{ form_rest(edit_form) }}
</form>
{% endblock content_body %}